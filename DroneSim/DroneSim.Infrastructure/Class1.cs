namespace DroneSim.Infrastructure
{
    public class Class1
    {
        // This project is the infrastructure layer of the drone simulation.
        // Middle layer between the API and the Core.
        // DroneService (implementation)
        // SignalR (if that's the one i finally use) and its hub
    }
}
